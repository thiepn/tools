function wrap(ctx:CanvasRenderingContext2D,text:string,maxWidth:number){const words=text.split(/\s+/),out:string[]=[];let line='';for(const word of words){const test=line?`${line} ${word}`:word;if(ctx.measureText(test).width>maxWidth&&line){out.push(line);line=word}else line=test}if(line)out.push(line);return out}
function b64(data:string){const raw=atob(data),out=new Uint8Array(raw.length);for(let i=0;i<raw.length;i++)out[i]=raw.charCodeAt(i);return out}
const ascii=(text:string)=>new TextEncoder().encode(text);
function join(parts:Uint8Array[]){const size=parts.reduce((n,p)=>n+p.length,0),out=new Uint8Array(size);let at=0;for(const p of parts){out.set(p,at);at+=p.length}return out}
export async function textToRasterPdf(text:string){const W=1240,H=1754,margin=105,lineH=42,canvas=document.createElement('canvas');canvas.width=W;canvas.height=H;const ctx=canvas.getContext('2d');if(!ctx)throw new Error('Canvas is unavailable.');ctx.font='28px system-ui, sans-serif';const lines:string[]=[];for(const raw of text.replace(/\r/g,'').split('\n')){if(!raw.trim())lines.push('');else lines.push(...wrap(ctx,raw,W-margin*2))}const perPage=Math.floor((H-margin*2)/lineH),images:Uint8Array[]=[];for(let p=0;p<Math.max(1,Math.ceil(lines.length/perPage));p++){ctx.fillStyle='#fff';ctx.fillRect(0,0,W,H);ctx.fillStyle='#111';ctx.font='28px system-ui, sans-serif';ctx.textBaseline='top';lines.slice(p*perPage,(p+1)*perPage).forEach((line,i)=>ctx.fillText(line,margin,margin+i*lineH,W-margin*2));images.push(b64(canvas.toDataURL('image/jpeg',.9).split(',')[1]))}return jpegPagesToPdf(images.map(bytes=>({bytes,width:W,height:H})))}
export function jpegPagesToPdf(images:{bytes:Uint8Array;width:number;height:number}[]){const count=images.length,objects:(Uint8Array|null)[]=new Array(3+count*3).fill(null);objects[1]=ascii('<< /Type /Catalog /Pages 2 0 R >>');objects[2]=ascii(`<< /Type /Pages /Count ${count} /Kids [${images.map((_,i)=>`${3+i*3} 0 R`).join(' ')}] >>`);for(let i=0;i<count;i++){const page=3+i*3,img=page+1,content=page+2,jpeg=images[i],pageW=595,pageH=Math.max(1,Math.round(pageW*jpeg.height/jpeg.width)),cmd=ascii(`q\n${pageW} 0 0 ${pageH} 0 0 cm\n/Im0 Do\nQ\n`);objects[page]=ascii(`<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageW} ${pageH}] /Resources << /XObject << /Im0 ${img} 0 R >> >> /Contents ${content} 0 R >>`);objects[img]=join([ascii(`<< /Type /XObject /Subtype /Image /Width ${jpeg.width} /Height ${jpeg.height} /ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${jpeg.bytes.length} >>\nstream\n`),jpeg.bytes,ascii('\nendstream')]);objects[content]=join([ascii(`<< /Length ${cmd.length} >>\nstream\n`),cmd,ascii('endstream')])}const parts=[ascii('%PDF-1.4\n%TinyTools\n')],offsets=[0];let offset=parts[0].length;for(let i=1;i<objects.length;i++){offsets[i]=offset;const part=join([ascii(`${i} 0 obj\n`),objects[i]!,ascii('\nendobj\n')]);parts.push(part);offset+=part.length}const xref=offset,rows=['0000000000 65535 f '];for(let i=1;i<objects.length;i++)rows.push(`${String(offsets[i]).padStart(10,'0')} 00000 n `);parts.push(ascii(`xref\n0 ${objects.length}\n${rows.join('\n')}\ntrailer\n<< /Size ${objects.length} /Root 1 0 R >>\nstartxref\n${xref}\n%%EOF`));return new Blob([join(parts)],{type:'application/pdf'})}
