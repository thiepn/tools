import{spawn,spawnSync}from'node:child_process';
import{once}from'node:events';
import{createServer}from'node:http';
import{mkdir,mkdtemp,readFile,rm,stat,writeFile}from'node:fs/promises';
import{tmpdir}from'node:os';
import path from'node:path';
import{createServer as createViteServer}from'vite';

const ROOT=process.cwd(),DIST=path.resolve(ROOT,'dist'),OUT=path.resolve(ROOT,'artifacts');
const HOST='127.0.0.1',PORT=4188,DEBUG_PORT=9238,BASE=`http://${HOST}:${PORT}/tools/`,EXPECTED=351;
const DESKTOP={width:1440,height:1000,mobile:false},MOBILE={width:360,height:800,mobile:true};
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
async function waitFor(check,label,timeout=18000){const end=Date.now()+timeout;let last;while(Date.now()<end){try{const v=await check();if(v)return v}catch(e){last=e}await sleep(70)}throw new Error(`Timed out waiting for ${label}${last?`: ${last.message}`:''}`)}
async function tools(){const vite=await createViteServer({root:ROOT,appType:'custom',logLevel:'error',server:{middlewareMode:true}});try{const tm=await vite.ssrLoadModule('/src/registry/tools.ts'),rm=await vite.ssrLoadModule('/src/registry/register-all.ts');rm.registerAllPublicTools();const rows=tm.TOOLS_REGISTRY;if(!Array.isArray(rows)||rows.length!==EXPECTED)throw new Error(`Expected ${EXPECTED} tools, got ${Array.isArray(rows)?rows.length:'non-array'}`);return rows.map(({id,name,description,category})=>({id,name,description,category}))}finally{await vite.close()}}
const MIME=new Map([['.html','text/html; charset=utf-8'],['.js','text/javascript; charset=utf-8'],['.mjs','text/javascript; charset=utf-8'],['.css','text/css; charset=utf-8'],['.json','application/json; charset=utf-8'],['.svg','image/svg+xml'],['.png','image/png'],['.jpg','image/jpeg'],['.jpeg','image/jpeg'],['.webp','image/webp'],['.wasm','application/wasm'],['.woff','font/woff'],['.woff2','font/woff2']]);
async function server(){const s=createServer(async(req,res)=>{try{const u=new URL(req.url??'/',`http://${HOST}:${PORT}`);if(u.pathname==='/favicon.ico'){res.writeHead(204).end();return}if(!u.pathname.startsWith('/tools')){res.writeHead(404).end('Not found');return}const rel=u.pathname==='/tools'||u.pathname==='/tools/'?'index.html':u.pathname.slice('/tools/'.length),candidate=path.resolve(DIST,rel),safe=path.relative(DIST,candidate);if(safe.startsWith('..')||path.isAbsolute(safe)){res.writeHead(403).end('Forbidden');return}const info=await stat(candidate),file=info.isDirectory()?path.join(candidate,'index.html'):candidate;res.writeHead(200,{'Content-Type':MIME.get(path.extname(file).toLowerCase())??'application/octet-stream','Cache-Control':'no-store'});res.end(await readFile(file))}catch(e){if(e?.code==='ENOENT')res.writeHead(404).end('Not found');else res.writeHead(500).end('Server error')}});await new Promise((r,j)=>{s.once('error',j);s.listen(PORT,HOST,r)});return s}
function chrome(){for(const c of[process.env.CHROME_BIN,'/usr/bin/google-chrome','google-chrome','google-chrome-stable','chromium','chromium-browser'].filter(Boolean))if(spawnSync(c,['--version'],{stdio:'ignore'}).status===0)return c;throw new Error('Chrome/Chromium not found')}
class Cdp{constructor(url){this.url=url;this.ws=null;this.id=1;this.pending=new Map;this.listeners=new Map}async open(){this.ws=new WebSocket(this.url);await new Promise((r,j)=>{this.ws.addEventListener('open',r,{once:true});this.ws.addEventListener('error',()=>j(new Error('CDP socket failed')),{once:true})});this.ws.addEventListener('message',e=>{const m=JSON.parse(e.data);if(m.id){const p=this.pending.get(m.id);if(!p)return;this.pending.delete(m.id);m.error?p.reject(new Error(m.error.message)):p.resolve(m.result);return}for(const h of this.listeners.get(m.method)??[])h(m.params??{})})}on(m,h){const a=this.listeners.get(m)??[];a.push(h);this.listeners.set(m,a)}send(method,params={}){const id=this.id++;return new Promise((resolve,reject)=>{this.pending.set(id,{resolve,reject});this.ws.send(JSON.stringify({id,method,params}))})}close(){if(this.ws?.readyState===WebSocket.OPEN)this.ws.close()}}
async function ev(cdp,expression){const x=await cdp.send('Runtime.evaluate',{expression,awaitPromise:true,returnByValue:true});if(x.exceptionDetails)throw new Error(x.exceptionDetails.exception?.description??x.exceptionDetails.text??'evaluation failed');return x.result?.value}
async function viewport(cdp,v){await cdp.send('Emulation.setDeviceMetricsOverride',{width:v.width,height:v.height,screenWidth:v.width,screenHeight:v.height,deviceScaleFactor:v.mobile?2:1,mobile:v.mobile})}
const INSPECT=id=>`(()=>{const shell=document.querySelector('[data-tool-id="${id}"]');if(!shell)return{missing:true};const core=shell.querySelector('.tt-tool-content');if(!core)return{missing:true};const clone=core.cloneNode(true);clone.querySelectorAll('[data-s-tier-workbench],[data-s-tier-a-console]').forEach(n=>n.remove());const visible=e=>{const s=getComputedStyle(e),r=e.getBoundingClientRect();return s.display!=='none'&&s.visibility!=='hidden'&&s.opacity!=='0'&&r.width>0&&r.height>0};const text=e=>(e.textContent||'').replace(/\\s+/g,' ').trim();const refs=v=>(v||'').trim().split(/\\s+/).filter(Boolean);const name=e=>{if(e.getAttribute('aria-label')?.trim())return true;const l=refs(e.getAttribute('aria-labelledby'));if(l.length&&l.every(x=>document.getElementById(x))&&l.some(x=>text(document.getElementById(x))))return true;if((e instanceof HTMLButtonElement||e instanceof HTMLAnchorElement)&&text(e))return true;if(e.id&&document.querySelector('label[for="'+CSS.escape(e.id)+'"]'))return true;if(e.closest('label'))return true;return false};const nodes=[...core.querySelectorAll('button,a[href],input:not([type="hidden"]),textarea,select')].filter(visible).filter(e=>!e.closest('[data-s-tier-workbench],[data-s-tier-a-console]'));const missingNames=nodes.filter(e=>!name(e)).length;const brokenRefs=[...core.querySelectorAll('[aria-labelledby],[aria-describedby]')].filter(e=>!e.closest('[data-s-tier-workbench],[data-s-tier-a-console]')).reduce((n,e)=>n+['aria-labelledby','aria-describedby'].reduce((s,a)=>s+refs(e.getAttribute(a)).filter(x=>!document.getElementById(x)).length,0),0);const ids=[...core.querySelectorAll('[id]')].filter(e=>!e.closest('[data-s-tier-workbench],[data-s-tier-a-console]')).map(e=>e.id),counts=ids.reduce((m,x)=>(m[x]=(m[x]||0)+1,m),{}),dup=Object.values(counts).filter(n=>n>1).length;const brokenLabels=[...core.querySelectorAll('label[for]')].filter(e=>!e.closest('[data-s-tier-workbench],[data-s-tier-a-console]')).filter(e=>!document.getElementById(e.getAttribute('for'))).length;const nested=[...core.querySelectorAll('button,a[href]')].filter(e=>!e.closest('[data-s-tier-workbench],[data-s-tier-a-console]')).filter(e=>e.querySelector('button,a[href],input,textarea,select')).length;const tiny=nodes.filter(e=>(e instanceof HTMLButtonElement||e instanceof HTMLAnchorElement)&&!text(e)&&e.querySelector('svg,img')).filter(e=>{const r=e.getBoundingClientRect();return r.width<24||r.height<24}).length;const unfocus=nodes.filter(e=>!e.hasAttribute('disabled')&&e.tabIndex<0).length;const body=(clone.innerText||clone.textContent||'').replace(/\\s+/g,' ').trim();const controls=[...clone.querySelectorAll('input:not([type="hidden"]),textarea,select')].length,actions=[...clone.querySelectorAll('button,a[href]')].length,outputs=[...clone.querySelectorAll('pre,code,table,canvas,svg,audio,video,img,[role="status"],[aria-live],output')].length,paras=[...clone.querySelectorAll('p,li')].map(text).join(' ').length,exportish=[...clone.querySelectorAll('button,a[href]')].filter(e=>/(copy|download|export|save|print|share)/i.test(text(e))).length,fileInputs=clone.querySelectorAll('input[type="file"]').length;const h1=[...shell.querySelectorAll('h1')].filter(visible).map(text);const suspicious=/\\b(?:NaN|undefined|Infinity)\\b|\\[object Object\\]/.test(body);const hardUnsupported=/^(?:.{0,80})(?:not supported|unsupported|unavailable in this browser)(?:.{0,80})$/i.test(body)&&nodes.length<2;return{missing:false,h1,bodyLength:body.length,controls,actions,outputs,paras,exportish,fileInputs,missingNames,brokenRefs,dup,brokenLabels,nested,tiny,unfocus,overflow:document.documentElement.scrollWidth>innerWidth+1||document.body.scrollWidth>innerWidth+1,errorBoundary:document.body.innerText.includes('Something went wrong in this tool'),suspicious,hardUnsupported,textSample:body.slice(0,300)}})()`;
const MUTATE=`(()=>{const shell=document.querySelector('[data-tool-id]'),core=shell?.querySelector('.tt-tool-content');if(!core)return{mutated:0,clicked:false};const aux=e=>Boolean(e.closest('[data-s-tier-workbench],[data-s-tier-a-console]'));const visible=e=>{const s=getComputedStyle(e),r=e.getBoundingClientRect();return s.display!=='none'&&s.visibility!=='hidden'&&r.width>0&&r.height>0};const set=(e,v)=>{const p=e instanceof HTMLTextAreaElement?HTMLTextAreaElement.prototype:e instanceof HTMLSelectElement?HTMLSelectElement.prototype:HTMLInputElement.prototype,s=Object.getOwnPropertyDescriptor(p,'value')?.set;s?s.call(e,v):e.value=v;e.dispatchEvent(new Event('input',{bubbles:true}));e.dispatchEvent(new Event('change',{bubbles:true}))};let n=0;for(const e of[...core.querySelectorAll('input:not([type="hidden"]),textarea,select')].filter(visible).filter(e=>!aux(e)).slice(0,8)){if(e.disabled||e.readOnly)continue;if(e instanceof HTMLSelectElement){const o=[...e.options].find((x,i)=>i>0&&!x.disabled)??[...e.options].find(x=>!x.disabled);if(o){set(e,o.value);n++}continue}if(e instanceof HTMLTextAreaElement){set(e,'Tiny Tools R18 verification sample 42');n++;continue}const t=(e.type||'text').toLowerCase();if(['file','hidden','submit','button','reset','image','radio'].includes(t))continue;if(t==='checkbox'){e.click();n++;continue}if(t==='number'||t==='range'){const min=e.min!==''&&Number.isFinite(Number(e.min))?Number(e.min):0,max=e.max!==''&&Number.isFinite(Number(e.max))?Number(e.max):100,cur=Number.isFinite(Number(e.value))?Number(e.value):min;set(e,String(Math.min(max,Math.max(min,cur+(Number(e.step)||1)))));n++;continue}if(t==='date'){set(e,'2026-09-05');n++;continue}if(t==='time'){set(e,'12:34');n++;continue}if(t==='datetime-local'){set(e,'2026-09-05T12:34');n++;continue}if(t==='month'){set(e,'2026-09');n++;continue}if(t==='week'){set(e,'2026-W36');n++;continue}if(t==='color'){set(e,'#2563eb');n++;continue}if(t==='email'){set(e,'audit@example.com');n++;continue}if(t==='url'){set(e,'https://example.com/audit');n++;continue}if(t==='tel'){set(e,'+491234567890');n++;continue}if(t==='password'){set(e,'R18!safe-local-test-42');n++;continue}set(e,'Tiny Tools R18 verification sample 42');n++}const safe=[...core.querySelectorAll('button')].filter(visible).filter(e=>!aux(e)&&!e.disabled).find(e=>/^(?:calculate|convert|format|generate|analyze|analyse|validate|check|compute|apply|preview|run|process|compare|solve|encode|decode|render)\b/i.test((e.textContent||'').trim())&&!/(camera|microphone|record|speed test|network|fetch|upload|download|delete|remove|clear|reset)/i.test((e.textContent||'')));if(safe){safe.click();return{mutated:n,clicked:true}}return{mutated:n,clicked:false}})()`;
function tier(score,cap=100){const s=Math.min(score,cap);return s>=93?'S':s>=86?'A':s>=76?'B':s>=66?'C':s>=50?'D':'F'}
function score(row,d,m,mut,autoExternal,postErrors){let correctness=0,robustness=0,ux=0,utility=0,privacy=0,compat=0,cap=100,notes=[];if(!d.missing&&!d.errorBoundary)correctness+=6;else{notes.push('desktop route/shell failure');cap=Math.min(cap,49)}if(d.h1?.includes(row.name))correctness+=4;else notes.push('canonical title mismatch');if(!postErrors.length)correctness+=6;else{notes.push('runtime/console errors');cap=Math.min(cap,75)}if(!d.suspicious)correctness+=4;else{notes.push('suspicious runtime output');cap=Math.min(cap,75)}if((mut.mutated||mut.clicked||d.fileInputs||d.actions)>0)correctness+=5;else notes.push('no observable task interaction');if(d.outputs>0||d.controls+d.actions>=3)correctness+=5;else notes.push('thin result surface');
if(!d.missing&&!d.errorBoundary)robustness+=5;if(!m.missing&&!m.errorBoundary)robustness+=5;else{notes.push('mobile route failure');cap=Math.min(cap,75)}if(!postErrors.length&&!d.suspicious)robustness+=5;if(!d.hardUnsupported&&!m.hardUnsupported)robustness+=5;else notes.push('hard browser capability failure');
if(d.missingNames===0&&m.missingNames===0)ux+=4;else{notes.push('unnamed controls');cap=Math.min(cap,92)}if(d.brokenRefs+d.dup+d.brokenLabels+d.nested+m.brokenRefs+m.dup+m.brokenLabels+m.nested===0)ux+=4;else{notes.push('semantic wiring defect');cap=Math.min(cap,92)}if(d.tiny+m.tiny===0&&d.unfocus+m.unfocus===0)ux+=3;else{notes.push('target/focus defect');cap=Math.min(cap,92)}if(!d.overflow&&!m.overflow)ux+=4;else{notes.push('horizontal overflow');cap=Math.min(cap,92)}
if(d.bodyLength>=260)utility+=4;else if(d.bodyLength>=160)utility+=3;else notes.push('thin core content');const density=d.controls+d.actions+d.outputs;if(density>=8)utility+=4;else if(density>=5)utility+=3;else if(density>=3)utility+=2;else notes.push('low task depth');if(d.paras>=120)utility+=3;else if(d.paras>=70)utility+=2;else notes.push('limited guidance');if(d.exportish>0||d.outputs>=2)utility+=2;else if(d.outputs>0)utility+=1;if(d.controls>=2||d.fileInputs||d.actions>=3)utility+=2;else if(d.controls||d.actions)utility+=1;
if(autoExternal.length===0)privacy=10;else{privacy=0;notes.push(`automatic external request (${autoExternal[0]})`);cap=Math.min(cap,65)}
const mount=d.mountMs??99999;if(mount<=1500)compat+=4;else if(mount<=3000)compat+=3;else if(mount<=5000)compat+=2;else{notes.push(`slow mount ${mount}ms`);cap=Math.min(cap,92)}if(!d.missing&&!m.missing)compat+=3;if(!d.hardUnsupported&&!m.hardUnsupported)compat+=3;
const total=correctness+robustness+ux+utility+privacy+compat,final=Math.min(total,cap);return{score:final,tier:tier(total,cap),dimensions:{correctness,robustness,ux,utility,privacy,compat},notes:[...new Set(notes)]}}
function md(report){const dist=Object.entries(report.distribution).map(([k,v])=>`| ${k} | ${v} |`).join('\n');const rows=report.results.map((r,i)=>`| ${i+1} | \`${r.id}\` | ${r.score} | **${r.tier}** | ${r.category} | ${r.notes.join('; ')||'—'} |`).join('\n');return`# R18 Final Independent 351-Tool Certification\n\nGenerated: ${report.generatedAt}\n\n## Independent rubric\n\nR18 deliberately does **not** award points for prior R15/R16/R17 tier labels. It grades observable production behavior after removing generic Expert Workspace / QA surfaces from depth accounting. Route-specific product functionality remains part of the product and is measured normally.\n\n- Correctness & task completeness: 30\n- Robustness & adversarial interaction: 20\n- UX, accessibility & mobile: 15\n- Utility & task depth: 15\n- Privacy / local-first behavior: 10\n- Compatibility & performance: 10\n\nTiers: S >= 93, A 86–92, B 76–85, C 66–75, D 50–65, F < 50. Critical correctness/privacy/accessibility defects impose tier caps.\n\n## Distribution\n\n| Tier | Count |\n|---|---:|\n${dist}\n\n**Certified S-tier:** ${report.certifiedS}/${report.total}\n\n## Complete rerank\n\n| # | Route | Score | Tier | Category | Findings |\n|---:|---|---:|:---:|---|---|\n${rows}\n`}
async function main(){await stat(path.join(DIST,'index.html')).catch(()=>{throw new Error('dist/index.html missing; run npm run build')});const rows=await tools(),s=await server(),profile=await mkdtemp(path.join(tmpdir(),'tiny-tools-r18-')),proc=spawn(chrome(),['--headless=new','--disable-gpu','--no-sandbox','--no-first-run','--no-default-browser-check','--disable-background-networking','--disable-component-update','--disable-sync','--metrics-recording-only',`--remote-debugging-port=${DEBUG_PORT}`,`--user-data-dir=${profile}`,'about:blank'],{stdio:['ignore','ignore','pipe']});let stderr='';proc.stderr.on('data',c=>stderr+=c.toString());try{await waitFor(async()=>(await fetch(`http://${HOST}:${DEBUG_PORT}/json/version`).catch(()=>null))?.ok,'Chrome DevTools',25000);const target=await(await fetch(`http://${HOST}:${DEBUG_PORT}/json/new?about%3Ablank`,{method:'PUT'})).json(),cdp=new Cdp(target.webSocketDebuggerUrl);await cdp.open();await Promise.all([cdp.send('Page.enable'),cdp.send('Runtime.enable'),cdp.send('Log.enable'),cdp.send('Network.enable')]);const errors=[],external=[];cdp.on('Runtime.exceptionThrown',({exceptionDetails})=>errors.push(exceptionDetails?.exception?.description??exceptionDetails?.text??'unknown exception'));cdp.on('Runtime.consoleAPICalled',({type,args})=>{if(type==='error'||type==='assert')errors.push((args??[]).map(a=>a.value??a.description??'').join(' ')||`console.${type}`)});cdp.on('Network.requestWillBeSent',({request})=>{try{const u=new URL(request.url);if(['http:','https:'].includes(u.protocol)&&u.hostname!==HOST)external.push(request.url)}catch{}});const results=[];console.log(`R18 independent certification: ${rows.length} routes`);for(let i=0;i<rows.length;i++){const row=rows[i];await viewport(cdp,DESKTOP);const e0=errors.length,x0=external.length,t0=Date.now();await cdp.send('Page.navigate',{url:`${BASE}#/tool/${row.id}`});await waitFor(()=>ev(cdp,`document.readyState==='complete'&&Boolean(document.querySelector('[data-tool-id="${row.id}"] .tt-tool-content'))`),`${row.id} desktop mount`);await ev(cdp,`new Promise(r=>requestAnimationFrame(()=>requestAnimationFrame(r)))`);const mountMs=Date.now()-t0,d0=await ev(cdp,INSPECT(row.id));d0.mountMs=mountMs;const autoExternal=external.slice(x0);const mut=await ev(cdp,MUTATE);await ev(cdp,`new Promise(r=>setTimeout(r,80))`);const d=await ev(cdp,INSPECT(row.id));d.mountMs=mountMs;const postErrors=errors.slice(e0);await viewport(cdp,MOBILE);const me0=errors.length;await cdp.send('Page.navigate',{url:`${BASE}#/tool/${row.id}`});await waitFor(()=>ev(cdp,`document.readyState==='complete'&&Boolean(document.querySelector('[data-tool-id="${row.id}"] .tt-tool-content'))`),`${row.id} mobile mount`);await ev(cdp,`new Promise(r=>requestAnimationFrame(()=>requestAnimationFrame(r)))`);const m=await ev(cdp,INSPECT(row.id)),mobileErrors=errors.slice(me0);const sc=score(row,d,m,mut,autoExternal,[...postErrors,...mobileErrors]);results.push({...row,...sc,evidence:{mountMs,controls:d.controls,actions:d.actions,outputs:d.outputs,mutated:mut.mutated,clicked:mut.clicked,autoExternal:autoExternal.length,desktopOverflow:d.overflow,mobileOverflow:m.overflow}});if((i+1)%25===0||i===rows.length-1)console.log(`✓ ${i+1}/${rows.length}`)}results.sort((a,b)=>b.score-a.score||a.id.localeCompare(b.id));const distribution={S:0,A:0,B:0,C:0,D:0,F:0};for(const r of results)distribution[r.tier]++;const report={generatedAt:new Date().toISOString(),baselineCommit:process.env.GITHUB_SHA??null,total:results.length,certifiedS:distribution.S,distribution,results};await mkdir(OUT,{recursive:true});await writeFile(path.join(OUT,'r18-final-certification.json'),JSON.stringify(report,null,2));await writeFile(path.join(OUT,'r18-final-certification.md'),md(report));console.log('R18 distribution:',distribution);console.log(`R18 S-tier certification: ${distribution.S}/${results.length}`);cdp.close();if(distribution.S!==EXPECTED){const holdouts=results.filter(r=>r.tier!=='S').slice(0,20).map(r=>`${r.id}:${r.score}/${r.tier}`).join(', ');throw new Error(`R18 certification incomplete: ${distribution.S}/${EXPECTED} S-tier. Holdouts: ${holdouts}`)}console.log('R18 FINAL CERTIFICATION PASSED: 351/351 S-tier under independent rubric')}catch(e){if(stderr.trim())console.error(`\nChrome stderr (tail):\n${stderr.slice(-3000)}`);throw e}finally{await new Promise(r=>s.close(r));if(proc.exitCode===null){proc.kill('SIGTERM');await Promise.race([once(proc,'exit'),sleep(1800)]);if(proc.exitCode===null)proc.kill('SIGKILL')}await rm(profile,{recursive:true,force:true,maxRetries:10,retryDelay:100})}}
main().catch(e=>{console.error(e instanceof Error?e.stack:e);process.exitCode=1});
